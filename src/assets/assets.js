const icon = {
    icon: {
        pokeball: require('./icon/pokeball.png').default,
        pokeball_color: require('./icon/pokeball_color.png').default,
        celebi: require('./icon/celebi.png').default
    }
}

export default icon;